android {
            defaultConfig {
                applicationId = "org.nativescript.newWHS"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }